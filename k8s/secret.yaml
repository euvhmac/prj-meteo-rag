# MeteoRAG — Secret Template
# NUNCA commitar valores reais aqui. Valores injetados via CI/CD.
apiVersion: v1
kind: Secret
metadata:
  name: meteorag-secrets
  namespace: meteorag
type: Opaque
data:
  METEORAG_ANTHROPIC_API_KEY: ""   # base64 — injetado via CI/CD
